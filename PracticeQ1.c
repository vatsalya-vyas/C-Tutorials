#include<stdio.h>
//Q) Write a C-Program to find Area of square ? 
int main(){
    int side;
    printf("Enter the side of a square : ");
    scanf("%d",&side);
    printf("The Area of square is : %d",side*side);
    return 0;
}