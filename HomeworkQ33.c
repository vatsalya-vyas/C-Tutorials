// Q)Search & find out which is better malloc( ) or calloc ( ).

// ANS)malloc is suitable when dynamic memory allocation is needed without the requirement for
// initializing memory to zero. It offers flexibility in allocating memory blocks of a specified size.
// On the other hand,
// calloc is preferable when initializing memory to zero is desired

// malloc() is faster