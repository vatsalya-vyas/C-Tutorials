#include<stdio.h>
// Q) Take a number(n) from user & output its cube(n*n*n).
int main(){
    int a;
    printf("Enter a number : ");
    scanf("%d",&a);
    printf("It's cube is : %d",a*a*a);
    return 0; 
}