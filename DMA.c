#include <stdio.h>
int main()
{
    printf("%d\n", sizeof(int)); // Size of operator is used to obtain size of datatype
    printf("%d\n", sizeof(float));
    printf("%d\n", sizeof(char));
    return 0;
}